# Changelog

## 0.1.3

- Publish notes for npm install via `dsh plugin add dsh-jiey-browser`.
- Align package metadata with the public GitHub repository.

## 0.1.2

- Harden MCP client error messages when Jiey Browser is offline.
- Keep `allowCookies` defaulted to false.

## 0.1.1

- Improve auto-discovery of the local Jiey MCP endpoint.
- Document download URL fallback when the browser is missing.

## 0.1.0

- Initial Cordis plugin with `browser_open`, `browser_navigate`, `browser_snapshot`, `browser_read`, `browser_act`, `browser_screenshot`, and `browser_tabs`.
